import { UniqueResponsiblesPipe } from './unique-responsibles.pipe';

describe('UniqueResponsiblesPipe', () => {
  it('create an instance', () => {
    const pipe = new UniqueResponsiblesPipe();
    expect(pipe).toBeTruthy();
  });
});
